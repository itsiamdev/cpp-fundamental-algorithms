#include <iostream>
using namespace std;

/*
    PROBLEM STATEMENT:

    Given two integers a and b, compute their difference (a - b).
*/

int main() {
    int a, b;
    cin >> a >> b;

    cout << a - b << endl;

    return 0;
}
